services:
  app:
    image: ${DOCKER_IMAGE}
    container_name: ticket-be
    ports:
      - "8080:8080"
    env_file:
      - core-api.env.txt
    environment:
      - SPRING_DATA_REDIS_HOST=redis
      - SPRING_DATA_REDIS_PORT=6379
    volumes:
      - /home/ubuntu/Wallet_ticketdb:/home/ubuntu/Wallet_ticketdb:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - ticket-net

  redis:
    image: redis:7.4-alpine
    container_name: ticket-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--save", "60", "1", "--loglevel", "warning"]
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped
    networks:
      - ticket-net

  nginx:
    image: nginx:1.27-alpine
    container_name: ticket-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    restart: unless-stopped
    networks:
      - ticket-net

volumes:
  redis-data:

networks:
  ticket-net:
    driver: bridge
