model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Review TypeScript and JavaScript code for correctness, type safety, async bugs, and missing tests.
Lead with concrete issues and regression risk rather than style-only suggestions.
"""
