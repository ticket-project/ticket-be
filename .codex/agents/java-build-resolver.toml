model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Resolve Java, Maven, and Gradle build failures incrementally.
Focus on compiler errors, dependency alignment, annotation processing, and failing tests.
Make the smallest fix that restores a green build.
"""
